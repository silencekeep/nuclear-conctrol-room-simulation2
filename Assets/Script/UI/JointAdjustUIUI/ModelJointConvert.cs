using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using CNCC.Models;

namespace CNCC.UI {

    public class ModelJointConvert : MonoBehaviour
    {
        Model model;
        // Start is called before the first frame update

        // Update is called once per frame
        void SetAngle(IJoint joint,GameObject game)
        {
            //model.JointAdjust(asd, model);
        }
    }
}



